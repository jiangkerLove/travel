<view class="boot">
  <view class="logo">结</view>
  <view class="h1">结伴出行</view>
  <view wx:if="{{!fail}}" class="sub">正在登录…</view>
  <block wx:else>
    <view class="err">{{message}}</view>
    <view class="retry" bind:tap="retry">重试</view>
  </block>
</view>
