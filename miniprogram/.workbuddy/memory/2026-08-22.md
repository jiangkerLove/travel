# 2026-08-22

## UI 改版（旅途计划小程序）
- 重构了全站视觉令牌：在 `app.wxss` 的 `page {}` 中定义了 emerald 主题（`--brand:#15a888` / `--brand-strong:#0e8c74` / `--brand-soft:#e6f6f0` / `--brand-grad` 渐变 / `--ink:#16241f` / `--muted:#7e8c84` / `--line:#eaf0ed` / 阴影与 `--radius`）。
- 将原 sage 绿（#6f9b88 / #f3f6f3 / #2f3d36 等）全量替换为新令牌，涉及 index / profile / travel home / bill / login / create / settle / plan / boot 等所有页面 wxss。
- `app.json` tabBar.selectedColor 改为 #15a888，导航栏背景改为 #f4f7f6。
- 首页（index）新增渐变 hero（含山丘 SVG）+ 数据概览条（进行中/同行伙伴/计划天数，由 list 派生，见 index.js 的 computeStat）。
- 高保真预览原型：`design-preview/redesign.html`（可浏览器打开，含 首页/详情/我的 三屏切换）。
- 注：原生 tabBar 图标为预置 PNG（assets/），未重绘。

## 视觉再调整：改为"小清新"方向（v3）
- 用户反馈 v2 emerald 太浓、渐变 Hero 太重。已整体下调饱和度与对比。
- 新令牌：主色 `#6cbfa6`（柔和薄荷）/ 深一档 `#4f9f8a` / 浅底 `#eef6f2`；`--brand-grad` 改为极淡浅晕染 `linear-gradient(135deg,#f1f9f5,#e6f3ed)`；页面背景 `#f8faf8`；阴影大幅减弱（sm 0.04 / md 0.05 / lg 0.08，rgba 70,100,90）。
- 首页 Hero 改为浅色卡片（淡渐变 + 深色字 + 柔薄荷山丘），缩略图改为 `#e8f4ef` 浅块 + 深色字；app.json 导航栏背景/选中色同步为 #f8faf8 / #4f9f8a。
- 用脚本把全站 12 个页面 wxss 的翡翠绿字面色统一降到新柔和色；已校验无残留浓绿、无浓重阴影。
- 预览原型 `design-preview/redesign.html` 同步为小清新版（浅 Hero、柔和封面色块、轻阴影）。

## 布局紧凑化（v4）
- 用户反馈"布局太大、臃肿"。收紧尺寸令牌：`--radius` 26rpx→22rpx；`.page` padding 24rpx→20rpx、`.card` padding 28rpx→22rpx、margin 24rpx→18rpx；阴影再减弱一档。
- 首页 index.wxss 重点收紧：侧边留白 32rpx→20rpx；Hero padding 44rpx→30rpx、标题 48rpx→38rpx；统计块 padding 22rpx→16rpx、数值 38rpx→32rpx；行程卡 padding 28rpx→20rpx、缩略图 96rpx→76rpx、间距 24rpx→18rpx；空状态 padding 80rpx→56rpx。
- 个人中心 profile.wxss：`.me-page` 32rpx→20rpx、user-card padding 32rpx→24rpx、头像 112rpx→96rpx、昵称 34rpx→30rpx、圆角 28rpx→22rpx。
- 旅途详情 travel/home.wxss：top-fixed 侧边留白 24rpx→20rpx（与首页统一）。
- 预览 `redesign.html` 同步收紧对应 px 间距/字号。

## 头部/按钮/卡片再压缩 + 标语改文案（v5）
- 用户反馈：头部占比太高、按钮太大、下面卡片也大、标语可改。
- 首页 hero 由"大图块（greet+title+sub+双按钮+山丘）"重构为**紧凑横条**：左文案（greet 22rpx + title 32rpx）/ 右单个主按钮"新建旅途"（去掉 min-width、padding 14rpx 26rpx）。删除 hero-sub、hero-actions、hero-hills。
- 加入入口改为 hero 下方的轻链接 `.join-row`（"输入邀请码，和同伴一起出发 ›"）。
- 标语文案：title「把路线排好，慢慢走 / 一起出发吧」→「下一站，去哪儿？」；greet「下午好，小江」。
- 行程卡再缩：padding 20rpx→18rpx、缩略图 76rpx→66rpx、标题 30rpx→28rpx、top 间距 18rpx→14rpx、meta margin 16rpx→12rpx。
- 统计块再缩：padding 16rpx 10rpx→14rpx 8rpx、数值 32rpx→28rpx、标签 20rpx→19rpx；stats margin-top 14rpx→12rpx。
- 预览 `redesign.html` 同步：hero 改 flex 横条、按钮 flex:0 0 auto 紧凑、join-row 轻链接、trip 封面 84rpx→76rpx、name 16rpx→15rpx、stats 收紧。

## 去掉大封面色块 + 标语重做（v6）
- 用户反馈：预览里行程卡片顶部"川/洱"大封面色块（76px 高）太突兀；标语"下午好，小江 / 下一站，去哪儿？"不好看。
- 预览 `redesign.html` 行程卡由"顶部大封面(cover ini+ttl) + body"改为**左侧小缩略图(46px 圆角)+ 右侧信息(trow/name/place/meta/count)**，与小程序 index.wxss 既有结构一致（去掉 .cover/.ini/.ttl/.body，新增 .thumb/.tmain/.trow）。
- 标语文案：去掉问候语与修辞问句，hero 改为「我的旅途」+ 副行数据「3 段进行中 · 还有 2 天出发」；小程序 index.wxml 同步（greet/title → title/sub，文案由 stat 数据驱动）。
- index.wxss：删除 .hero-greet，新增 .hero-sub（22rpx muted）。

## 去 logo + 顶部轻量化（v7）
- 用户反馈：行程卡"川/洱"首字母 logo 不需要；顶部区域可再优化。
- 行程卡去掉左侧首字母缩略图（.thumb / .trip-top），改为**纯文字列表行**（.trip-main 直接承载 title-row/place/date/count + meta）。小程序与预览一致。
- 顶部 hero 卡片（背景渐变+边框+阴影）改为**无背景轻栏 .topbar**：标题「我的旅途」+ 右侧「＋ 新建」描边按钮（.add-btn/HTML .add），去掉副行数据（与 stats 三块重复）。
- 预览 `redesign.html` 同步：.hero→.topbar、.trip 去 .thumb/.tmain 改纯文字行。

## 还原原版骨架 + 小清新润色（v8 · 关键转向）
- 用户反馈：去掉视觉元素后"不好看，不如原版"。诊断：v3–v7 一路做减法，把原版成立的结构（完整头部 + 带缩略图行程卡）削没了，变成光秃秃纯白文字。
- 方向确认（AskUserQuestion）：用户选「原版骨架 + 润色」—— 还原原版结构，仅叠柔和配色/轻阴影/克制间距。
- 还原 `pages/index/index.wxml` 到原版骨架：hero（brand「旅途计划」+ lead 标语 + 双按钮 新建旅途/加入）+ section + 带缩略图行程卡；标语改为更利落的「把行程排好，把花销记清」。
- `pages/index/index.wxss` 重写：保留原版布局，润色点——缩略图 88rpx→76rpx（避免"那么大一块"）、品牌字 48rpx→44rpx、按钮 min-width140→132/padding 微调、卡片 padding 28rpx→24rpx / margin 24rpx→18rpx、阴影用令牌 --shadow-sm（更轻）；配色沿用 v3 薄荷绿令牌。
- 预览 `redesign.html` 同步回原版骨架：home 改回 brand+lead+双按钮 hero、trip 带 40px 缩略图；删掉 v7 的 topbar/stats/join-row。详情/我的 两屏保持不变。
- 经验：该用户对"小清新"的定义是"原版结构 + 柔和润色"，不是"删元素"。后续优化优先保留视觉骨架，避免继续做减法。

## 用户回退全部修改后重新润色（v9 · 最终采纳方向）
- 用户将 v2–v8 全部 git 回退到原版，要求重新润色。本次严格"只动视觉层，零结构改动"。
- `app.wxss`：TDesign 品牌色阶（--td-brand-color-1..10）从蓝改为薄荷绿色阶（7=#4f9f8a）；page{} 背景改 #f8faf8、--brand/#4f9f8a、新增 --brand-soft；.card 阴影改绿灰调更轻；.chip 从蓝改薄荷绿。
- `app.json` + 15 个页面级 json：导航/页面背景 #F5F7FB、#f3f6f3 → #F8FAF8；tabBar selectedColor #0052D9(蓝) → #4F9F8A、未选中 #93A39B。
- 13 个页面 wxss 用脚本做纯色值映射（sage→mint）：#6f9b88→#6cbfa6、#4f6f62/#5d8a76→#4f9f8a、#f3f6f3→#f8faf8、#2f3d36→#2f3b35、#8a9a90→#8fa19a 等 20 项；间距/字号/布局零改动（git diff 验证仅色值 + 1 处增量阴影）。
- 唯二非色值改动：首页标语「一起把路线排好，慢慢走」→「把行程排好，把花销记清」；.soft-btn.main 加 rgba(108,191,166,.28) 柔和投影。
- 预览 `redesign.html` 同步：--brand-soft/#e9f6f0、--muted/#8fa19a、.btn.solid 加投影，与代码像素级一致。
- 校验：全站（wxss/json/app.json）无 #6f9b88/#f3f6f3/#0052d9 等旧色残留；临时脚本已删。

## 加大"小清新"可见度（v10 · 用户反馈 v9 不明显）
- 用户反馈 v9 润色"感觉不明显"，要更小清新。策略：结构依旧不动，叠加看得见的淡彩元素。
- 首页 hero 从纯文字改为**淡薄荷晕染卡片**：linear-gradient(135deg,#eaf7f1,#f6fbf8) + 1rpx #e3f2ea 边框 + 右上角 radial 光晕（.hero-blob，rgba(124,202,180,.22)）；内容/字号/间距保持原版。
- 行程卡缩略图改**淡彩轮换**：wxml 用 `c{{index % 4}}`，wxss 定义 .thumb.c1 蜜桃(#fdeee4/#d08a5e)、.c2 天空(#e4f1fb/#5e93c7)、.c3 奶油(#fbf4dd/#c0a04a)，c0 默认薄荷。
- 主按钮 .soft-btn.main 改柔和渐变 linear-gradient(135deg,#7ccab4,#56ab92)。
- 章节标题加渐变小色条 .sec-dot（wxml 增 view）；空状态改淡渐变底+细边框；卡片阴影统一绿调 rgba(108,191,166,.07~.09)。
- 全站背景从 #f8faf8 调成薄荷调 **#f4f9f6**（app.wxss + app.json + 15 个页面 json + 12 个页面 wxss 的 page bg / active 态）。
- 预览 `redesign.html` 同步：hero 卡+blob、渐变按钮、thumb p1/p2/p3 淡彩、sec-title::before 色条、--bg #f4f9f6、trip 阴影绿调。
- 小清新配方（该项目语境）：原版结构 + 薄荷主色 + 淡彩点缀（蜜桃/天空/奶油）+ 极淡渐变晕染 + 绿调轻阴影。

## 实际看效果 + 真正修复（v11 · 关键发现）
- 用户再次反馈"不好看"，决定真正看一眼。用 Chrome headless 截图 `design-preview/redesign.html` 审查。
- **根因发现**：预览文件长期未与代码对齐——预览用了独立旧 class（`.trow/.sub/.count` 带 `background:#f3faf7` 绿色块、`.sep`、无 `.thumb`），而代码用的是 `.trip-head/.trip-sub/.trip-count/.soft-pill`。导致之前所有"小清新"调整在预览里看不到。**教训：不要凭代码记忆改预览，必须先截图验证对齐。**
- 实际修复：
  - 重写预览 HOME CSS（`.thumb`+`.head`+`.sub`+`.body` 布局）、markup 严格对齐 `pages/index/index.*`。
  - 给预览截图确认：发现 hero 太空、count 绿色块突兀，缩略图缺失。
- 代码侧补完 v10 设想但 wxml 漏掉的部分：
  - `index.wxml`：trip 加 `<view class="trip-thumb c{{index%4}}">` 缩略图，hero 加内联 SVG 山+太阳细线插画。
  - `index.wxss`：新增 `.trip-thumb` (76rpx + 3 色淡彩 c1/c2/c3，c0 默认薄荷)、`.trip` 改 flex 横排、`.trip-body` 包裹右侧内容；`.trip-count` 去掉 padding/bg/radius，改纯文字；`.hero-blob` 透明度 .22→.32 提一点，`.hero-art` svg 定位。
  - `index.js`：新增 `iconFor(dest)` 给每段行程按目的地关键字打 emoji（山→🏔️、海→🌊、岛→🏝️、城→🏛️、寺→⛩️、花→🌸、雪→❄️、夜→🌃、默认 ✈️），decorate 写入 `item.icon`。**避免了之前用户反感的"川/洱首字母 logo"，改用目的地 emoji。**
- 预览补充第三张行程（🌸 京都赏樱）让画面饱满，展示完整淡彩轮换（薄荷/蜜桃/奶油）。
- 截图（`home2.png`）确认：hero 插画 + emoji 缩略图 + 渐变按钮 + 章节色条全部呈现，整体"清新且有设计感"。
- **方法论沉淀**：UI 改版工作流必须包含"截图自审"环节，不能只看代码改；预览与代码必须定期截图验证同步。

## 用户反馈"别改我的字段" + 还原内容层（v12 · 重要边界）
- 用户："有些元素，比如还有多少天出发，还有多少工作日这些字段，你不能改我的字段啊"（"游戏"是"有些"的口误）。
- 核查结论：**字段/计算逻辑一个没动**——`index.js` 仅 15 行新增（iconFor+icon）、0 删除；`countdown: t.countdown || ''` 原样透传；`pages/profile`、`pages/mine` 的 js 零改动（工作日/退休计算未碰）。
- 但发现一处**显示层**改动让用户误以为字段被改：原版（HEAD）`.trip-count` 本身带绿色块背景 `background:#f3faf7`，v11 被我去掉了 → "还有多少天出发"显示变样。已**原样还原**绿色块。
- 彻底退守内容层底线：
  - `index.wxml` 行程卡结构 100% 还原 HEAD（去掉 `.trip-thumb` emoji、`.trip-body` 包裹）。
  - `index.js` 删除 `iconFor` 函数与 `icon` 字段 → `index.js` 与 HEAD **0 差异**。
  - `index.wxss`：`.trip` 去掉 flex 改回块级 + 原版 padding；删 `.trip-thumb/.trip-body`；`.trip-count` 还原绿色块。
  - 预览 `redesign.html` 同步：trip 去 thumb/body、count 还原绿色块。
- 唯一保留的"新增元素"：hero 的 `hero-blob`（光晕）+ `hero-art`（山景 SVG）——纯装饰非字段，已告知用户可一键去除。
- **关键教训（写进项目约定）**：该用户的底线是"字段/数据/计算逻辑绝不能动，字段的原有显示样式也尽量别改"。后续润色只做：纯配色、纯阴影、新增装饰元素需先征得同意。改样式前必须 `git show HEAD` 确认该元素原版样式，避免误删原版特征（如 count 绿色块）。

## 新功能：行程卡 SVG 插画 + 选择器（v13 · 用户主动要求）
- 用户："下面的卡片也可以优化一下，给每个旅程让选一个svg插画也不错"。这是用户授权的新功能，不算动既有字段。
- 新增 `assets/illust/` 下 10 个薄荷线条风 SVG：mountain/beach/island/city/forest/desert/temple/lake/snow/balloon（viewBox 80×80，描边 #4f9f8a/#56ab92/#7ccab4/#9bd3b6）。
- 新增 `utils/illust.js`：导出 `ILLUST` 选项表、`defaultIllustFor(dest)`（按目的地关键字默认匹配）、`getIllust(id,dest)`/`setIllust(id,key)`（本地存储 `illust_<id>`）、`illustSrc(key)`。**不动后端字段，选择存本地。**
- `pages/index/index.js`：`decorate` 新增派生字段 `illust`（getIllust 读取，纯前端）；新增 `pickIllust`/`chooseIllust`/`closeIllustSheet` 处理器 + data(`illustSheet`/`illustPickingId`/`illustOptions`)。既有字段全部未动。
- `pages/index/index.wxml`：行程卡左侧加 `.trip-illust`（淡彩 c0-c3 轮换 + `<image src="/assets/illust/{{item.illust}}.svg">`），用 `catch:tap="pickIllust"`（点缩略图换插画、不触发 openTrip）；内容包入 `.trip-body`；页面根级加插画选择面板（mask + bottom sheet + 10 项网格）。
- `pages/index/index.wxss`：`.trip` 改 flex；`.trip-illust` 84rpx 圆角淡彩块；`.trip-body` 包裹；附 `.illust-mask/.illust-sheet/.illust-grid/.illust-opt` 面板样式（底部弹出 + safe-area）。
- 预览 `redesign.html` 同步：行程卡加内联 SVG 插画（川西=mountain、大理=lake、京都=temple）+ 淡彩底。
- 注：本轮 Chrome headless 截图在沙箱内连续失败（内存受限），未能自审截图；预览面板可直观查看。WeChat `<image>` 支持 .svg 渲染。

## 简化：去掉选择器，只留 logo 图位（v14 · 用户纠偏）
- 用户："你把卡片优化一下就行了，留一个logo图片的地方就可以，后面我看可以直接根据路线图来生成这样的一个svg图片不"。
- 即：选择器做重了，只要卡片上留个图片位；后面用户会自己用"路线图生成的 SVG"填进去。
- 删除：`index.wxml` 的插画选择面板（mask+sheet+grid）、`index.js` 的 `pickIllust/chooseIllust/closeIllustSheet` 与 `illustSheet/illustPickingId/illustOptions` data、`index.wxss` 的 `.illust-*` 面板样式、`utils/illust.js` 的 `getIllust/setIllust` 存储函数。
- 保留：卡片左侧 `.trip-illust`（淡彩 c0-c3 + `<image src="/assets/illust/{{item.illust}}.svg">`）；`decorate` 用 `defaultIllustFor(t.destination)` 给默认占位；10 个 SVG 文件保留作占位/参考。
- **后续接入路线图生成图**：把 `decorate` 里 `item.illust` 改成生成图的路径/URL（或新增 `item.cover` 字段），并把 wxml `<image src>` 绑过去即可，结构不用动。
- 校验：`index.js`/`index.wxml` 无选择器残留引用；`trip-illust` 图位与样式保留。
